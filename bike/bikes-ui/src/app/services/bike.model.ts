export interface IBike{
    id: number, 
    name: string, 
    email: string,
    phone: string,
    model: string,
    serialNumber: string, 
    purchasePrice: number, 
    purchaseDate: Date, 
    contact: boolean,
}